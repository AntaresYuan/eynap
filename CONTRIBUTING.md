# 一起改 / Contributing

欢迎 Issue 和 Pull Request。

## 先看这条：改动该提到哪里

| 你要改的 | 提到哪 |
|---|---|
| 方法论本身：价值锚点怎么挖、PRD 怎么组织、调研怎么设计 | [上游仓库](https://github.com/SmileLiuuuu/ai-pm)，那是源头 |
| 拆分结构、共享地基、单技能可用性、宿主适配 | 本仓库 |
| 不确定属于哪边 | 先开个 Discussion 问一句 |

## 提 Issue

三类，说清楚哪一类就行：

- **用下来哪里卡住了** — 附上你说的原话和它的回应。具体场景比"效果不好"有用得多。
- **想加一个技能或能力** — 先说你要解决的问题，再说设想的做法。
- **文档不对或读不懂** — 指出具体是哪一段。

## 提 Pull Request

改之前值得知道的几条约定：

**1. 横切规则只在 `shared/` 存一份**

记忆、偏好、命名、协议、宿主差异、措辞规范都在 `shared/`，所有技能引用它。
不要为了方便把内容复制进某个技能目录——那会让两份慢慢长歪。

**2. 每个技能要能单独成立**

`skills/<技能>/SKILL.md` 的 frontmatter 里，`description` 决定它什么时候被调起。
写清楚触发场景，也写清楚它不负责什么。缺上游文档时，技能要能自己把必要信息一次问齐，
不要挤牙膏式地来回追问。

**3. 措辞规范**

`shared/verification.md` 里有一份反例词表。简单说：不用大词、不用隐喻、不用黑话。
"赋能""抓手""心智""对齐颗粒度"这类词不要出现在产出里，也不要出现在文档里。

**4. 改了脚本就跑一遍**

```bash
bash scripts/init_project.sh test-proj /tmp/eynap-test
bash scripts/check_gate.sh /tmp/eynap-test
```

两个脚本都用 `bash` 调用——打包解压后执行位可能丢失。

## 许可

提交贡献即表示同意以 CC BY-NC 4.0 授权。这是继承自上游的许可，不能改成更宽松的。
